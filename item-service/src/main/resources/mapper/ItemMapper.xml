<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.hmall.item.mapper.ItemMapper">

    <update id="restoreStock">
        update item
        <set>
            stock = case
            <foreach collection="items" item="num">
                when id = #{item.itemId} then stock + #{item.num}
            </foreach>
            end
        </set>
        where id in
        <foreach collection="items" open="(" separator="," close=")">
            #{item.itemId}
        </foreach>
    </update>

</mapper>